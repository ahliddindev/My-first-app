name: Build AAB
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build with Buildozer
        uses: NekoSekai/buildozer-action@v4
        with:
          command: buildozer android bundle
